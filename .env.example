# Google Suite CLI - Environment Configuration
# Copy this file to .env and fill in your values

# Option 1: JSON content directly in environment variable
# GOOGLE_CREDENTIALS='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...@...iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}'

# Option 2: Path to service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Or use the --credentials-file flag when running commands:
# gsuite --credentials-file /path/to/service-account.json ...

# Required: User to impersonate (must be set via --user flag)
# The service account must have domain-wide delegation enabled
# and the target user must be in the Google Workspace domain
# gsuite --user user@yourdomain.com whoami
